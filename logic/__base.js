const validPost =async  (obj) => {
    return true;
}

const validPut =async  (obj) => {
    return true;
}

const  validDelete =async (obj) => {
    return true;
}

const WebHooks = {
    POST : "",
    PUT: "",
    DELTE : ""
}

module.exports = {
    validPut,
    validDelete,
    validPost,
    WebHooks
}